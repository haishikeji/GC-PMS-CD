import { AppDisabledDirective } from './app-disabled.directive';

describe('AppDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
